package command;

/**
 * Интерфейс для классов команд.
 *
 * @author AstroSoup
 */
public interface Command {
    /**
     * Запускает цикл выполнения конкретной команды.
     */
    void execute();
}
